{
  "name": "traveller-chargen",
  "source": [
    "./src/index.html"
  ],
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "npx rimraf build dist",
    "test": "vitest",
    "test:run": "vitest run",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "format:check": "biome format src test",
    "lint": "biome lint src test --skip=lint/style/noDescendingSpecificity",
    "check": "npm run typecheck && npm run format:check && npm run lint && npm run test:run && npm run build"
  },
  "dependencies": {
    "@ksilvennoinen/markov-namegen": "^2.0.0",
    "@ksilvennoinen/urw-classico": "^1.0.0",
    "@ksilvennoinen/urw-classicsans": "^1.0.0",
    "@picocss/pico": "^2.0.6",
    "@svgdotjs/svg.js": "^3.2.0",
    "pure-rand": "^8.4.2",
    "temporal-polyfill": "^1.0.4"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^5.0.0",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
